/* @flow */
import React from 'react'
import { Text, ScrollView } from 'react-native'
import type { ScreenProps } from '../../types'

export default function Settings({ navigation }: ScreenProps) {
  return <ScrollView><Text>Settings</Text></ScrollView>
}
